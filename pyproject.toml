[project]
name = "agent-memory-system"
version = "0.1.0"
description = "Self-Improving Agent Memory & Decision System MVP"
requires-python = ">=3.10"
dependencies = [
    "networkx>=3.0",
    "chromadb>=0.4.0",
    "pydantic>=2.0",
    "langgraph",
    "langchain",
    "litellm",
    "numpy",
    "python-dotenv"
]

[tool.setuptools]
packages = ["src"]
